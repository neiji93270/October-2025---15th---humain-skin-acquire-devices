//as we dont use ui/ux for anlong period and as
//we dont use the right corne bottom of the camera images, we can use rrace of nxnxn model if we can use 5,7 nulber of theeshold. then everything deals with diffuse sctaering then
//we need a high range of colors if we use the nxntrace of the mateixes resulting.
//this mezn that we can use the bottom.right of the images if
//we can use 5600 values hasardous. the distribution is not gaussian because
//we cannot use odd values. so we need a model mnist
